<div class="navbar-btn navbar-right tac">
  <span class="btn-group">
    <a href="{{route 'auth.register' lang=locale}}" class="btn btn-default">{{i18n 'ui_register'}}</a>
    <a href="{{route 'auth.login' lang=locale}}" class="btn btn-default">{{i18n 'ui_signin'}}</a>
  </span>
  <a href="{{route 'game.index' lang=locale}}" class="btn btn-lead">
  {{{i18n 'play'}}}
  </a>
</div>
